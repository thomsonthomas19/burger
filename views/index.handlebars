<h1 class="text-center display-2">Burgers!</h1>

<h2 class="text-center display-3">Burgers that are waiting to be eaten!</h2>

<ul>
  {{#each burgers}}
    {{#unless eaten}}
      {{> burgers/burger-block sleep=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2 class="text-center display-3">Burgers that have been eaten!</h2>

<ul>
  {{#each burgers}}
    {{#if eaten}}
      {{> burgers/burger-block sleep=false}}
    {{/if}}
  {{/each}}
</ul>

<h2 class="text-center display-4">Add a Burger</h2>
<form class="create-form text-center">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <button class="btn btn-danger" type="submit">Add Burger</button>
</form>
